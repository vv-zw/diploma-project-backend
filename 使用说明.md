# 使用说明

## 1. 项目当前实现

当前推荐系统已经升级为：

- 推荐模型：`NCF + TextCNN + 规则特征融合`
- 刷新机制：`异步刷新任务 + 状态轮询 + 缓存读取`
- 用户偏好：按 `movie` 与 `series` 分桶管理

这意味着：

- 电影推荐只使用电影偏好计算
- 剧集推荐只使用剧集偏好计算
- 刷新按钮不再同步等待模型运行完成
- 刷新完成后页面会自动更新当前推荐列表

## 2. 环境要求

建议使用：

- Python `3.12`
- 虚拟环境：`venv312`
- TensorFlow 已安装

启动前可先确认：

```bash
venv312\Scripts\activate
python --version
python -c "import tensorflow as tf; print(tf.__version__)"
```

## 3. 启动方式

### 方式一：批处理启动

直接运行：

```text
启动应用-Python312.bat
```

### 方式二：命令行启动

```bash
venv312\Scripts\activate
python movie_recommendation\app.py
```

启动成功后，后端默认运行在：

```text
http://127.0.0.1:5000
```

## 4. 推荐接口说明

### 4.1 读取推荐结果

接口：

```text
GET /get_recommend?type=movie
GET /get_recommend?type=series
```

说明：

- 该接口现在只读取缓存推荐结果
- 不会同步重新训练或生成推荐
- 用于页面快速展示当前推荐内容

### 4.2 发起刷新任务

接口：

```text
POST /refresh-recommendations
```

请求示例：

```json
{
  "type": "movie"
}
```

返回示例：

```json
{
  "code": 0,
  "message": "refresh_queued",
  "type": "movie",
  "job_id": "movie_20260313_154500_ab12cd34",
  "status": "queued"
}
```

说明：

- 该接口只创建后台刷新任务
- 会立即返回，不会阻塞前端

### 4.3 查询刷新状态

接口：

```text
GET /refresh-status?job_id=...
```

返回状态：

- `queued`
- `running`
- `done`
- `failed`

前端会轮询该接口，直到刷新任务完成。

## 5. 小程序页面刷新逻辑

### 电影推荐页

文件：

- [movieRecommend.js](/d:/pythonProjectmovie，tobacco/微信小程序/pages/movieRecommend/movieRecommend.js)

当前逻辑：

1. 首次进入页面时调用 `GET /get_recommend`
2. 点击“刷新”时调用 `POST /refresh-recommendations`
3. 获得 `job_id` 后轮询 `GET /refresh-status`
4. 任务完成后重新调用 `GET /get_recommend`
5. 当前页直接更新推荐内容

### 剧集推荐页

文件：

- [showRecommend.js](/d:/pythonProjectmovie，tobacco/微信小程序/pages/showRecommend/showRecommend.js)

当前逻辑与电影页一致：

1. 点击刷新后先创建后台任务
2. 再轮询任务状态
3. 任务完成后重新读取缓存推荐
4. 页面无需退出重进即可更新内容

## 6. 用户偏好与分桶权重

当前系统已按内容类型分别管理用户偏好：

- `preferences.movie`
- `preferences.series`

同时分别计算：

- `count_weights.movie`
- `count_weights.series`

这样可以保证：

- 电影偏好不会污染剧集推荐
- 剧集偏好不会污染电影推荐

## 7. 推荐算法说明

当前主链路是：

```text
NCF + TextCNN + 规则分
```

最终分数：

```text
final_score =
0.35 * ncf_score +
0.30 * textcnn_score +
0.20 * genre_score +
0.10 * quality_score +
0.05 * diversity_score
```

同时保留：

- 黑名单过滤
- 不感兴趣惩罚
- 多样性重排

## 8. 降级策略

为保证系统稳定性，当前保留如下降级链路：

1. `NCF + TextCNN + 规则分`
2. `NCF + 规则内容相似度 + 规则分`
3. `TextCNN + 规则分`
4. `规则内容推荐 + 多样性重排`

因此在 TensorFlow、NCF 或 TextCNN 不可用时，系统仍可以返回推荐结果。

## 9. 常见情况说明

### 9.1 刷新按钮点击后没有立刻返回推荐

这是正常现象。

当前刷新逻辑是异步的：

- 点击刷新后，页面会显示“正在刷新推荐，请稍候...”
- 后端在后台生成推荐
- 任务完成后页面自动更新

### 9.2 剧集推荐没有明显个性化

如果剧集偏好为空，系统虽然不会再错误复用电影权重，但剧集个性化能力会有限。

建议：

- 在前端补充剧集偏好
- 同步到后端后再重新刷新剧集推荐

### 9.3 日志中出现 TensorFlow 提示

如以下日志：

```text
oneDNN custom operations are on
cpu_feature_guard.cc
```

这些通常是信息提示，不表示程序失败。

### 9.4 日志中出现 `input_length is deprecated`

这是 Keras 的弃用警告，不影响当前运行，但后续可以清理 `TextCNN.py` 中的旧参数写法。

## 10. 当前版本说明

当前推荐实现版本可视为：

```text
NCF_TextCNN_v3.0
```

主要特性：

- 异步刷新
- 分桶权重
- 主链路深度融合推荐

## 11. 后续建议

建议后续继续完善：

1. 将新主链路接入离线评估脚本
2. 为剧集补充独立偏好样本
3. 将刷新任务状态持久化
4. 增加推荐解释字段

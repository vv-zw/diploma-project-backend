# 使用 Python 3.12 + TensorFlow 运行项目

## ✅ 当前状态

- ✅ Python 3.12 已安装
- ✅ Python 3.14 已安装
- ✅ venv312 虚拟环境已创建（使用 Python 3.12）

---

## 🚀 方法 1: 使用批处理文件（最简单）

### 首次安装 TensorFlow

双击运行：
```
安装TensorFlow.bat
```

这个脚本会自动：
1. 激活 Python 3.12 虚拟环境
2. 升级 pip
3. 安装项目依赖
4. 安装 TensorFlow
5. 验证安装

**预计时间**: 10-20 分钟

### 启动应用

双击运行：
```
启动应用-Python312.bat
```

这个脚本会：
1. 激活 Python 3.12 虚拟环境
2. 启动应用

---

## 🖥️ 方法 2: 使用命令行（手动操作）

### 首次安装

打开 CMD 或 PowerShell，在项目目录下运行：

```bash
# 激活虚拟环境
venv312\Scripts\activate

# 升级 pip
python -m pip install --upgrade pip

# 安装依赖
pip install -r requirements-minimal.txt

# 安装 TensorFlow（使用国内镜像，速度快）
pip install tensorflow -i https://pypi.tuna.tsinghua.edu.cn/simple

# 验证安装
python -c "import tensorflow as tf; print('✅ TensorFlow 版本:', tf.__version__)"
```

### 以后每次启动

```bash
# 激活虚拟环境
venv312\Scripts\activate

# 运行应用
python movie_recommendation\app.py
```

---

## 🔍 验证安装

### 检查 Python 版本

激活虚拟环境后：
```bash
python --version
```

应该显示：`Python 3.12.x`

### 检查 TensorFlow

```bash
python -c "import tensorflow as tf; print(tf.__version__)"
```

应该显示：`2.15.0` 或类似版本号

### 运行应用测试

```bash
python movie_recommendation\app.py
```

**成功标志**：
```
✅ 成功加载配置文件
✅ 成功导入config模块
✅ 成功加载配置文件
✅ 成功加载所有模块
✅ 使用NCF模型生成推荐（基于X个偏好）  ← 关键！
* Running on http://127.0.0.1:5000
```

**不应该看到**：
```
⚠️ TensorFlow/Keras未安装，将使用基于内容的推荐方案
```

---

## 🔄 环境切换

### 使用 Python 3.12 + TensorFlow

```bash
venv312\Scripts\activate
python movie_recommendation\app.py
```

或双击：`启动应用-Python312.bat`

### 使用 Python 3.14（无 TensorFlow）

```bash
# 确保没有激活虚拟环境
deactivate

# 直接运行
python movie_recommendation\app.py
```

### 退出虚拟环境

```bash
deactivate
```

---

## 📁 项目文件说明

### 虚拟环境
- `venv312/` - Python 3.12 虚拟环境文件夹

### 批处理文件
- `安装TensorFlow.bat` - 自动安装 TensorFlow
- `启动应用-Python312.bat` - 使用 Python 3.12 启动应用

### 文档
- `使用说明.md` - 本文件
- `切换到Python312.md` - 详细的切换指南
- `快速安装TensorFlow.md` - 快速安装指南
- `安装步骤详解.md` - 详细安装步骤

---

## ⚠️ 常见问题

### Q1: 双击批处理文件没反应？

**解决**：
1. 右键批处理文件
2. 选择 "以管理员身份运行"

或者在 CMD 中手动运行命令。

---

### Q2: 激活虚拟环境后 python --version 还是 3.14？

**原因**：虚拟环境创建有问题

**解决**：
```bash
# 删除虚拟环境
rmdir /s venv312

# 重新创建
py -3.12 -m venv venv312

# 激活
venv312\Scripts\activate

# 验证
python --version
```

---

### Q3: pip install tensorflow 失败？

**错误**: `ERROR: Could not find a version that satisfies the requirement tensorflow`

**原因**：虚拟环境中的 Python 不是 3.12

**解决**：
```bash
# 在虚拟环境中检查版本
python --version

# 如果不是 3.12.x，重新创建虚拟环境
deactivate
rmdir /s venv312
py -3.12 -m venv venv312
venv312\Scripts\activate
```

---

### Q4: 安装 TensorFlow 速度很慢？

**解决**：使用国内镜像源

```bash
# 清华镜像（推荐）
pip install tensorflow -i https://pypi.tuna.tsinghua.edu.cn/simple

# 阿里镜像
pip install tensorflow -i https://mirrors.aliyun.com/pypi/simple/

# 中科大镜像
pip install tensorflow -i https://pypi.mirrors.ustc.edu.cn/simple/
```

---

### Q5: 导入 TensorFlow 报错？

**错误**: `ImportError: DLL load failed`

**解决**：
1. 安装 Visual C++ Redistributable:
   - 下载: https://aka.ms/vs/17/release/vc_redist.x64.exe
   - 安装后重启电脑

2. 重新安装 TensorFlow:
   ```bash
   pip uninstall tensorflow
   pip install tensorflow
   ```

---

## 📊 性能对比

| 特性 | Python 3.14 | Python 3.12 + TensorFlow |
|------|-------------|--------------------------|
| 推荐算法 | 基于内容 | NCF 神经协同过滤 |
| 相似度计算 | 关键词匹配 | TextCNN 深度学习 |
| 推荐质量 | 良好 | 更好 |
| 个性化程度 | 中等 | 高 |
| 启动速度 | 快 | 稍慢（首次训练） |

---

## 🎯 推荐工作流程

### 日常开发

1. 双击 `启动应用-Python312.bat`
2. 或在 CMD 中：
   ```bash
   venv312\Scripts\activate
   python movie_recommendation\app.py
   ```

### 更新依赖

```bash
venv312\Scripts\activate
pip install --upgrade tensorflow
pip install -r requirements-minimal.txt --upgrade
```

### 清理环境

```bash
# 退出虚拟环境
deactivate

# 删除虚拟环境（如果需要重新创建）
rmdir /s venv312
```

---

## ✅ 检查清单

安装完成后确认：

- [ ] venv312 文件夹已创建
- [ ] 激活虚拟环境后 `python --version` 显示 3.12.x
- [ ] TensorFlow 已安装
- [ ] `import tensorflow` 不报错
- [ ] 运行应用看到 "使用NCF模型生成推荐"
- [ ] 应用启动时没有 TensorFlow 警告
- [ ] 可以正常访问 http://127.0.0.1:5000

---

## 📞 需要帮助？

如果遇到问题：
1. 查看本文档的"常见问题"部分
2. 查看 `安装步骤详解.md` 获取更详细的说明
3. 查看 `切换到Python312.md` 了解环境切换

---

**最后更新**: 2026-03-08  
**Python 版本**: 3.12.x  
**TensorFlow 版本**: 2.15.x
